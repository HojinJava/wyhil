## 공통 프롬프트

[feature-definitions/{SLUG}.md]({FEATURE_DEF_URL})

---

## 참여 모델 & 작업 규칙

이슈를 확인한 후 본인 모델에 해당하는 브랜치를 생성하고 PR을 제출하세요.  
추가 프롬프트 없이 공통 프롬프트만으로 진행합니다.

| 모델 | 브랜치 | PR 제목 |
|------|--------|---------|
{MODEL_RULES_TABLE}

---

## 작업 프로세스 (모델 공통)

1. 위 표에서 본인 브랜치 생성 후 바이브코딩 진행
2. 변경사항 커밋 & 푸시
3. 아래 PR 생성 규칙에 따라 PR 생성
4. PR 본문에 LLM 사용정보 및 `References \#{ISSUE_NUMBER}` 반드시 포함
5. md 파일 작성 시 Markdown 작성 규칙 반드시 준수

---

## PR 생성 규칙

**⚠️ 아래 규칙을 반드시 준수합니다. 위반 시 평가에서 제외될 수 있습니다.**

| 항목 | 규칙 |
|------|------|
| 브랜치 | `vibe/{model-slug}/{SLUG}-{ISSUE_NUMBER}` |
| PR 제목 | `[{모델명}] {TITLE_CLEAN} \#{ISSUE_NUMBER}` |
| PR → Issue 연결 | PR 본문에 `References \#{ISSUE_NUMBER}` 포함 |
| Closes 사용 | ❌ 금지 — 이슈는 모든 모델 완료 후 관리자가 수동으로 닫음 |
| LLM 사용정보 | PR 본문에 반드시 포함 (아래 양식 참고) |
| Markdown 규칙 | md 파일 작성 시 아래 Markdown 작성 규칙 준수 |

---

## LLM 사용정보

**⚠️ PR 생성 전 반드시 작성하세요. 미작성 시 평가에서 제외될 수 있습니다.**

| 항목 | 내용 |
|------|------|
| 태스크 | |
| 모델명 | |
| 소요 시간 | |
| 프롬프트 횟수 | |
| 인풋/아웃풋 토큰 | (확인 불가 시 N/A) |
| 추가 프롬프트 | (없으면 "없음") |
| 수동 수정 여부 | |
| 지침서 | (없으면 "없음") |
| 자체 완성도 | /5 |

---

## 리뷰어 가이드

이 PR을 리뷰할 모델은 아래 템플릿을 참고하여 평가서를 작성하고 PR 댓글로 등록합니다.

→ [리뷰어 템플릿 보기](https://github.com/{OWNER}/{REPO}/blob/main/.claude/skills/vibe-eval/templates/review-body.md)

---

## Markdown 작성 규칙

**⚠️ md 파일 작성 시 아래 규칙을 반드시 준수합니다. 위반 시 렌더링 오류가 발생합니다.**

| 금지 | 대신 사용 | 이유 |
|------|-----------|------|
| `> 텍스트` (블록쿼트) | 평문으로 작성 | GitHub에서 핑크 세로라인 렌더링 |
| `#숫자` (예: `#1`) | `\#숫자` (예: `\#1`) | GFM 오토링크로 밑줄 전파 |
| `<!-- 주석 -->` | 주석 제거 후 평문 작성 | 렌더링 오작동 가능성 |
